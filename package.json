{
  "name": "fullstack-project",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run build --workspace frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
{
  "name": "fullstack-project",
  "private": true,
  "workspaces": ["public", "api"],
"scripts": {
    "dev": "concurrently \"npm run dev --workspace api\" \"npm run build --workspace public\"",
    "build": "npm run build --workspace api && npm run build --workspace public",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}